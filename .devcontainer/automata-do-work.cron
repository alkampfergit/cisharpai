# Run one Automata work tick every 5 minutes inside the dev container.
# /etc/cron.d format: the field after the schedule is the user to run as.
*/5 * * * * vscode /usr/local/bin/automata-do-work-cron >> /home/vscode/.local/state/automata-do-work/cron.log 2>&1
